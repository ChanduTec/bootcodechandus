<configuration>
	<appender name="RoolingFile"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>logger_implemenation.log</file>
		
		<encoder>
			<pattern>%d[%thread] %-5level %logger{40} - %msg%n
			</pattern>
		</encoder>
		
		<rollingPolicy
			class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
			<fileNamePattern>logger_implemenation.log-%d{yyyy-MM-dd}.%i.log.gz
			</fileNamePattern>
			<!-- each archived file's size will be max 10MB -->
			<maxFileSize>10MB</maxFileSize>
			<!-- 30 days to keep -->
			<maxHistory>30</maxHistory>
			<!-- total size of all archive files, if total size > 100GB, it will delete 
				old archived file -->
			<totalSizeCap>100GB</totalSizeCap>
		</rollingPolicy>
		
	</appender>

	<root level="INFO">
		<appender-ref ref="RoolingFile" />
	</root>
</configuration>